# --- Telegram ---
aiogram==3.13.1

# --- HTTP ---
# L'extra http2 tire `h2` : parler HTTP/2 comme un navigateur évite une partie
# des filtres anti-bot, qui remarquent un « Chrome » qui négocie du HTTP/1.1.
# brotli : ~20 % de moins sur le fil que gzip. socks : proxies socks5://.
httpx[http2,brotli,socks]==0.27.2

# --- Config / validation ---
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
